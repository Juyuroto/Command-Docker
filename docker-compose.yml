services:
  frontend:
    container_name: docker-command
    build:
      context: ./frontend
      dockerfile: dockerfile
    restart: unless-stopped
    ports:
      - 3006:3006
    volumes:
      - ./frontend:/app:Z
      - /app/node_modules
    environment:
      - "${FRONTEND_PORT}:5000"